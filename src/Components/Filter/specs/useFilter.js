import useFilter from "../useFilter"

describe("useFilter", () => {
    const handleFilter = (arg) => { console.log(arg) }
    const { showClear, findColor, findSize, clearAll } = useFilter({ handleFilter })

    describe("findColor", () => {
        it("", () => {

            //findBySingleColor(color);
            //     setShowClear(true);
            //handleFilter(result);
        })
        it("makes 'showClear' true", () => {

            //findBySingleColor(color);
            //     setShowClear(true);
            //handleFilter(result);
        })
    })

    describe("findSize", () => {
        it("", () => {

            //findBySingleColor(color);
            //     setShowClear(true);
            //handleFilter(result);
        })
        it("makes 'showClear' true", () => {

            //findBySingleColor(color);
            //     setShowClear(true);
            //handleFilter(result);
        })
    })


    describe("clearAll ", () => {
        it("removes all the filters", () => {

            //findBySingleColor(color);
            //     setShowClear(true);
            //handleFilter(result);
        })

        it("displays the full product list", () => {

            //findBySingleColor(color);
            //     setShowClear(true);
            //handleFilter(result);
        })

        it("makes 'showClear' false", () => {

            //findBySingleColor(color);
            //     setShowClear(true);
            //handleFilter(result);
        })
    })
})