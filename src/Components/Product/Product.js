const Product = (id, name, color, img, price, category, texture, salesRate, img2) => {
    return {
        id: id,
        name: name,
        color: color,
        img: img,
        price: price,
        category: category,
        texture: texture,
        salesRate: salesRate,
        img2: img2
    }
}

export default Product
